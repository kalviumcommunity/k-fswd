// The data-generator.js file contains a function that generates data for users and cart.
// const generateData = require('./data-generator');
// const { users, cart } = generateData(20);
// console.log(users);
// console.log(cart);

function transformData(users, cart) {
  // 1. Add a new property to the users array called cart
  // 2. Assign the cart array to the cart property as per the userId
  // 3. Return the users array
  return users
}

module.exports = transformData;
