const SECRET = 'expert';

const encryptHS256 = (payload) => {
  // your code here

  return token;
};

const encryptRS256 = (payload) => {
  // your code here

  return token;
};

module.exports = { SECRET, encryptHS256, encryptRS256 };
