// import the require module

// read from the pocess.env and assign the values
const config = {
  api_key: '',
  secret: '',
  is_kalvian: process.env.IS_KALVIAN ?? false
}

// assign the config to module.exports
module.exports = {}
